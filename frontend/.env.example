# Point the dashboard at your deployed backend (set these in Vercel/Render env).
# Local dev needs neither — vite.config.ts proxies /api and /ws to localhost:8000.
VITE_API_URL=https://caliber-api.onrender.com
VITE_WS_URL=wss://caliber-api.onrender.com/ws/live
